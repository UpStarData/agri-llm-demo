# AgriLink 农链 · V0.6 演示站

| 项目 | 值 |
| --- | --- |
| 版本 | **V0.6**（第三轮修改 + 第五轮补充：体量口径 / 地图缩放三项规则 / 铺满整屏 / 毛玻璃 / 统一红点 / 克制动效 / 直播卡常驻） |
| 源提交 | `llm-291/v1-fact-relation-redo` @ `010b42d`（构建自 `e6c236a`） |
| 构建指纹 | `window.__V03_BUILD = { hash: "60dd9ba775e5", git: "e6c236a" }` |
| 产物 | `index.html`（约 7.0 MB 单文件，CSS/JS/echarts/hls.js/地图 geo/字体/数据全部内联） |
| 数据 | 934 条结构事实（含 OurAirports 900 个大型机场、Open-Meteo 37 条天气、重点农产品 36 条）+ 体量层采样（每类型 1万/10万/100万 配额） |
| 版本地址约定 | 每次发版**新开一个带版本号的地址**，旧版本保留可回溯：`-v04`（暗色版·已否决留档）→ `-v05`（浅色视觉校准）→ **`-v0.6`（本次）** |

## 回滚 / 下线

```bash
gh repo delete UpStarData/agri-llm-demo-v0.6
gh api -X DELETE repos/UpStarData/agri-llm-demo-v0.6/pages
```
