# AgriLink V1.0 · GitHub Pages 发布产物

本分支（`gh-pages`）只包含**已验收的构建产物**，用于公开发布演示站；业务源码在
`llm-291/v1-fact-relation-redo` 分支，`main` 未被改动。

| 项目 | 值 |
| --- | --- |
| 源提交 | `57e6582`（llm-291/v1-fact-relation-redo） |
| 构建指纹 | `window.__V03_BUILD = { hash: 013d9c717e3d, git: 57e6582 }` |
| 产物 | `index.html`（约 6.0 MB，CSS / JS / echarts / 地图 geo / 4 档字重 / 数据包全部内联，零外部请求依赖） |
| 数据包 | `agrilink-demo-v1`（861 事实 / 377 本体 / 585 关系 / 64 产区 / 56 港口 / 49 机场 / 4 节点 / 1135 时序事件） |
| 验收 | `node test/v03.mjs` → 92/92 通过（`test/report-v03.md`） |
| 密钥 | 产物已扫描：无 API key / token / 本地绝对路径 / 内网地址；仓库仅登记 `.env.example` |

## 发布与回滚

- 发布地址：`https://upstardata.github.io/agri-llm-demo-v04/`（独立仓库 `UpStarData/agri-llm-demo-v04`，Pages 源 `main` / `/`）。
- **不触碰**原站 `https://upstardata.github.io/agri-llm-demo/`（仓库 `UpStarData/agri-llm-demo`，Pages 源 `main@46be371`，
  已实测逐字节一致：SHA256 `32c6b5da394a769e247c186920e51d9fbc39b3f336d7a2a88eb1193ac1065604`）。
- 回滚：删除本发布仓库（`gh repo delete UpStarData/agri-llm-demo-v04`）或关闭其 Pages
  （`gh api -X DELETE repos/UpStarData/agri-llm-demo-v04/pages`）；本仓库 `main` 只有一个发布提交，
  回退到任意历史提交即可。原站不受影响。
- 重新发布：在 `llm-291/v1-fact-relation-redo` 上 `node build.mjs && node test/v03.mjs`，
  然后把 `index.html` 与 `build-meta.json` 覆盖到本仓库 `main` 并推送。
