# AgriLink V0.8.1

LLM-291 地图锚定补丁：将遗漏的新事实瞬时闪点从绝对定位 DOM 覆盖层迁入 ECharts `geo` series。事实层现在不存在任何独立于地图投影的点图层；持久事实点、密度点、影响圆、扩散波纹和瞬时闪点均由同一坐标系渲染。

- 专项验收：`npm run test:v081`
- 判定方式：比较最终 Canvas 图元与 geo 投影，而非只比较理论坐标
- DPR=2、连续拖动 / 缩放后五类图层最大漂移：`0px`
