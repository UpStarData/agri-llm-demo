# V0.4c 公网发布记录（AgriLink V1.0 · 源提交 57e6582）

## 两个地址

| 角色 | 地址 | 说明 |
| --- | --- | --- |
| 原版 Demo（**保持不变**） | https://upstardata.github.io/agri-llm-demo/ | 仓库 `UpStarData/agri-llm-demo`，Pages 源 `main` / `/`，线上版本 `main@46be371`；公网实测 SHA256 `32c6b5da394a769e247c186920e51d9fbc39b3f336d7a2a88eb1193ac1065604`，与 `main@46be371:index.html` **逐字节一致**（2,456,820 字节） |
| 新版演示（本次发布） | https://upstardata.github.io/agri-llm-demo-v04/ | 新仓库 `UpStarData/agri-llm-demo-v04`，Pages 源 `main` / `/`，发布提交 `main@026494d`；产物 = `57e6582` 的验收构建，公网实测 SHA256 `41b732157567dab9a82935c3ca761a2cfbea5de18709e04e2cfb1ef6c4551bb8`（与本地验收产物逐字节一致） |

## 发布方式

- 采用**独立仓库 + 独立 Pages 站点**：不修改原仓库 `main`、不修改原仓库 Pages 源、不影响原站内容。
- 发布分支只含三个文件：`index.html`（6.0 MB 单文件离线产物）、`build-meta.json`、`PUBLISH.md`（溯源与回滚说明）。
- 构建指纹：`window.__V03_BUILD = { hash: 013d9c717e3d, git: 57e6582, partial: false }`。
- Pages 构建记录：`built 026494d 2026-09-21T22:44:39Z (39.1s)`。

## 回滚

```bash
# 方式一：删除发布站点（原站不受影响）
gh repo delete UpStarData/agri-llm-demo-v04
# 方式二：只关闭 Pages
gh api -X DELETE repos/UpStarData/agri-llm-demo-v04/pages
```

发布仓库 `main` 仅有一个发布提交，回退到其它提交或强推历史版本同样可行。

## 核验

`node test/live-check-v04.mjs https://upstardata.github.io/agri-llm-demo-v04/` → 16/16 通过
（构建指纹 / 数据包指纹 / 默认 7 天+高可信+高影响 / 默认口径 77 点多区域 / 全部 342 点 /
关联层 377 节点 585 关系 / 390px 无溢出 / 控制台无错误），报告见 `test/report-live-v04.md`。
