# 农业数据三层推演平台 V0.3 · 事实层 / 关联层 / 推演层（Demo）

在浅色基线 [AgriLink](https://upstardata.github.io/agri-llm-demo/)（原提交 `46be371`）的**代码与视觉体系**上改写的新 Demo。
信息架构只有一个业务链条：**事实层 → 关联层 → 推演层**；报告是推演层的结果页，不是第四个 TAB。

> ⚠️ 全部数值为**示意数据（待标定）**，仅用于确认展示形态与信息结构；口径字段为真实口径框架。
> 推演层为**离线示意引擎**，不是真实 MiroFish 后端运行；Jev 仅作为旁路模型信号展示接入状态与降级原因（本页不接收、不记录、不提交任何密钥）。

## 启动

```bash
npm install          # 仅 Playwright 用于测试与截图
npm run build        # 生成单文件 index.html（离线可用，直接双击即可）
npm run serve        # 本地演示服务（默认 127.0.0.1:4173，静态托管同一份文件）
```

构建产物就是仓库根目录的 `index.html`（约 2.3 MB，CSS/JS/地图 geo/字体全部内联，无任何外部请求）。

## 主路径

`事实层总览（地图） → 事实详情与证据 → 携带事实进入关联层 → 关系图谱 / 地理关联 → 关系详情与回溯 → 加入推演种子 → 推演配置与七阶段 → 轮次过程 → 报告与引用 → 回到对应事实 / 轮次`

推演层为统一用户故事（演示样例）：**马来西亚榴莲流入红星市场的占比** 与 **红星市场在湖南榴莲交易量的市场份额**，贯穿五阶段十二步骤。

## 三层各做什么

| 层 | 主体 | 关键交互 |
| --- | --- | --- |
| 事实层 | 地图（点 + 影响范围） | 时间/分类/二级/来源/可信度/影响度筛选、空间下钻（全球 → 中国 → 省区）、事实卡片（图文/视频/公开直播流）、事实详情（影响范围、证据、相关本体对象与关系）、加入推演种子、底部数据接入与处理流水 |
| 关联层 | 关系图谱 / 地理关联双视图 | 九类对象域分类、对象与关系详情（强度/置信度/形成时间/最近变更事实/支撑事实/时序记忆）、社区摘要、携带事实高亮、无坐标对象清单、底部本体抽离与关联处理流水 |
| 推演层 | 五阶段承载十二步骤 + 12 轮 + 报告 + 深度追问 | 统一用户故事「马来西亚榴莲 → 红星市场份额」；两个核心指标显示分子/分母/最近 30 天/交易量口径/35% 演示基线；种子确认、开始/暂停/单步/回放/重置、run 隔离（`AGRI-DURIAN-HX-001`）、农业角色 Agent、参数来源标签、逐轮指标与信号、报告分离 现实基线/模型推断/模拟结果/建议 并带引用、深度追问可新建 run（HX-002）、Jev 旁路信号面板（可模拟降级）、离线回放、失败重试 |

## 目录

```
src/v03/            当前 Demo 的全部实现（构建内联这些文件）
  shell.html        三 TAB 骨架
  app.css           设计令牌 + 骨架 + 事实层样式
  data.js           事实 / 本体对象 / 关系（示意数据）
  data-sim.js       三条样例场景 · 七阶段 · 轮次 · 报告 · 旁路信号
  store.js          唯一状态源（所有交互写回这里）
  filter.js         三层共用的唯一过滤实现
  fact.js / relation.js / sim.js   三层实现
  relation.css / sim.css
docs/               设计映射与验收清单、模块接口契约、基线对照与组件复用说明
test/v03.mjs        端到端测试（56 项，含状态断言与离线校验）
test/shots-v03.mjs  截图取证（桌面 1440 + 窄屏 390）
test/compare-v03.mjs 基线对照取证（同视口左右等宽对照图）
shots/              截图产物（test/ 子目录为测试运行时的截图）
legacy/             改写前的 AgriLink L1–L4 基线（不参与构建，仅对照）
```

## 验证

```bash
npm test          # 68 项端到端断言（Playwright / file:// 离线 / 桌面 + 390px）→ test/report-v03.md
npm run shots     # 三层截图 → shots/
npm run compare   # 基线 46be371 与 V0.3 的 1440px 对照截图 → shots/baseline、shots/v03、shots/compare
```

测试口径：每个交互都读 `window.V03Store.state` 判定是否真的写回状态，不接受「只换文案」；同时校验无控制台错误、无外部网络请求。
